/**
 * LO QUE DICE LA PÁGINA HTML.
 *
 * Cambiar cualquiera de estas frases no toca ni una línea de código.
 * Si quieres que una página HTML concreta tenga su propio título en el
 * índice, ponle un `<title>` al archivo: el libro lo lee de ahí.
 */

export default {
  /** Cómo se llama su grupo en el índice. */
  acto: "Contigo",

  /** Título de una página que no trae `<title>`. `%` es el número. */
  sinTitulo: "Página %",

  /** Mientras el archivo llega. Dura un suspiro; casi nunca se ve. */
  abriendo: "abriendo…",

  /** Si el archivo no se puede abrir. */
  rota: "Esta página no se pudo abrir.",

  /** Y debajo, en pequeño, para él y no para ella. */
  rotaPista: "revisa el archivo en la carpeta paginas-html/",

  /** Pista de la primera, para que sepa que esto también se pasa. */
  primeraVez: "desliza para seguir · o usa las flechas",
};
